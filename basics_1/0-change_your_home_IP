#!/usr/bin/env bash
# Script that configures localhost to 127.0.0.2 and facebook.com to 8.8.8.8

sudo cp /etc/hosts /etc/hosts.backup
sudo sed -i 's/^127\.0\.0\.1\s\+\(.*localhost.*\)/127.0.0.2 \1/' /etc/hosts
sudo grep -q 'facebook.com' /etc/hosts \
    && sudo sed -i 's/.*facebook\.com.*/8.8.8.8 facebook.com/' /etc/hosts \
    || echo '8.8.8.8 facebook.com' | sudo tee -a /etc/hosts > /dev/null
